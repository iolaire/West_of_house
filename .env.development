# Development environment variables
# These are used during local development (npm run dev)

# API timeout (10 seconds for faster feedback during development)
VITE_API_TIMEOUT=10000

# Disable analytics in development
VITE_ENABLE_ANALYTICS=false

# Note: API base URL is automatically configured from amplify_outputs.json
# For local development, use the sandbox environment
