# Production environment variables
# These are used during the build process for production deployments

# API timeout (30 seconds)
VITE_API_TIMEOUT=30000

# Enable analytics in production
VITE_ENABLE_ANALYTICS=true

# Note: API base URL is automatically configured from amplify_outputs.json
# The Amplify build process will use the deployed backend API endpoint
